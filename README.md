# LLVM DAG to yEd GraphML Converter

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Python 3.x](https://img.shields.io/badge/python-3.x-blue.svg)](https://www.python.org/)

[English](#english) | [中文说明](#中文说明)

<a name="english"></a>
## 🇬🇧 English

An AI-Generated powerful Python tool specifically designed for compiler developers to convert LLVM SelectionDAG Graphviz `.dot` files into yEd-compatible `.graphml` files.

AI Prompt：

```
first round:
I used the LLVM llc tool to generate the DOT file corresponding to the DAG graph. However, directly converting it with the Graphviz dot tool only produces a static image. I need to adjust the nodes and edges to make the graph visually appealing. Please write a script to convert the DOT file into a format that can be imported into other tools for drag-and-drop editing.
I know yEd Live supports editing DOT files, but it does not support the styling of DOT graphs. The result looks drastically different and unpolished compared to the image generated by the Graphviz dot tool, as shown in the partial comparison above. I have also provided my DOT file for your reference on the format.

second round:
I used dot2xml.py to convert the DOT file to GraphML. Although the GraphML file can be imported correctly, the visual effect does not meet my expectations:
Figure 1 (generated by the dot tool): The DAG nodes contain complete information including node IDs, inputs, outputs, operators, etc.
Figure 2 (imported into yEd): Only the node IDs are displayed, which does not satisfy my requirements.
The original file is main.dot, and the converted output is main.graphml. Based on this, please refine the code to fulfill my requirements.

third round:
main.dot is my source DOT file, and the images are examples of the node style I need. Now I need you to write a script to extract the label definition and all displayed node information from the DOT file, so that I can export this complete data into the corresponding GraphML nodes.
Required Node Content:
Operation Nodes:
Inputs (0,1,2,3), Operator (e.g., RISCVISD::CALL), Node ID (t1, t2...), Outputs (type, ch, glue)
Constants, Frame Index, Global Address, and Other Symbols:
Symbol definition (e.g., TargetConstant<0>, RegisterMask, Register $x10, TargetGlobalAddress<i64 (i64,i64)* @add> 0 [TF=1], FrameIndex<1>), Node ID (t1, t2...), Outputs (type, ch, glue)
Please implement this with a script. Note the conversion between the label formatting in the DOT file and the actual displayed information.
(Note: The online version of yEd currently does not support reconstructing node labels into formatted HTML tables.)
```

When using LLVM's `llc` tool to generate DAG dot files, directly viewing them as images lacks editability. Existing converters often fail to parse LLVM's complex `Mrecord` formats and lose edge styles. This script solves that by structuring the node labels into beautiful HTML tables and preserving all layout and styling information for seamless drag-and-drop editing in [yEd Graph Editor](https://www.yworks.com/products/yed).

(Note: The online version of yEd currently does not support reconstructing node labels into formatted HTML tables.)

### ✨ Features
*   **Intelligent Record Parsing**: Automatically parses LLVM's messy `{<s0>0...}` strings into structured fields: Inputs, Operator, Node ID, and Outputs.
*   **Native HTML Tables**: Renders nodes as native HTML tables in yEd, perfectly matching the original Graphviz visual structure.
*   **Preserves Layout & Styles**: Extracts node coordinates from Graphviz and scales them for better readability. Fully restores LLVM edge styles (e.g., `blue/dashed` for control flow, `red/bold` for Glue/Chain).
*   **Auto Size Policy**: Nodes dynamically resize to wrap their internal text perfectly.

### 🛠 Prerequisites
*   Python 3.x
*   [Graphviz](https://graphviz.org/) installed and `dot` added to your system's PATH.

### 🚀 Usage

```bash
python3 dot2xml.py input.dot output.graphml
```

1. Generate your LLVM DAG dot file (e.g., via `llc -view-isel-dags`).
2. Run the script.
3. Open `output.graphml` in yEd.

### 📂 Examples
Check out the `/examples` directory for a sample `main.dot` generated by LLVM and the resulting `main.graphml`.

---

<a name="中文说明"></a>

## 🇨🇳 中文说明

使用AI生成专为编译器开发者打造的 Python 工具，用于将 LLVM SelectionDAG 生成的 Graphviz `.dot` 文件转换为 yEd 可编辑的 `.graphml` 文件。

AI提示词：

```
first round:
我使用llvm的llc工具为我生成了DAG图对应的dot文件，但直接使用Graphviz的dot工具对其进行转换的话生成的是一张图片，而我需要对其中的节点、线进行调整，使其美观。为我撰写对应的脚本将dot文件转为利于其他工具导入进行拖拽编辑的格式。还有我知道，yEd Live 支持对dot文件编辑，但它不支持dot图像的样式，与Graphviz的dot工具转换出来的图片，相差很大且不美观，如上局部图的对比；还有我提供的dot文件供你参考dot文件的格式

second round:
使用dot2xml.py转换dot文件为graphml，虽然graphml文件可以正确导入了，但图像效果不符合我的预期：
图1分别是dot工具转换生成的DAG图，节点包含的内容不只是节点编号还有节点的信息（输入，输出，操作符等等），图2则为导入yEd的结果，只有节点编号信息，不符合我的需求。
main.dot为原dot文件，main.graphml为转换后的输出，根据这些信息
继续完善代码，实现我的要求

third round:
main.dot是我的dot文件，图片都是我需要节点样式的示例 ，现在我需要你使用脚本我提取各个节点在dot文件的内容定义label和对应呈现在图片中节点的信息，方便后续输出到对应的graphml的节点中，节点内容：
操作节点：输入(0,1,2,3)、操作符号（RISCVISD::CALL）、节点编号（t1、t2...)、 输出（类型、ch、glue）
常量、帧索引全局变量等符号：作为常量、帧索引全局变量等符号存在时的符号定义（TargetConstant<0>、RegisterMask、Register $x10、TargetGlobalAddress<i64 (i64,i64)* @add> 0 [TF=1]、FrameIndex<1>)、节点编号（t1、t2...)、 输出（类型、ch、glue）

使用脚本为我进行提取，注意dot文件中节点内容label中的格式与真实信息的转换。
```

在使用 LLVM 的 `llc` 工具生成 DAG 图时，直接导出为图片无法进行二次拖拽编辑。现有的转换工具往往无法正确解析 LLVM 复杂的 `Mrecord` 格式，且会丢失依赖边（如 Glue、Chain）的颜色和样式。本工具通过 AST 级别的解析，将节点标签重构为精美的 HTML 表格，并完美保留所有布局坐标与连线样式，方便在 [yEd Graph Editor](https://www.yworks.com/products/yed) 中进行深入分析与排版。

(注意：线上的yEd目前不支持对节点标签重构为精美的 HTML 表格)

### ✨ 核心特性
*   **智能 Record 解析**：自动将 LLVM 杂乱的 `{<s0>0...}` 字符串解构为：输入 (Inputs)、操作符 (Operator)、节点 ID 和输出 (Outputs)。
*   **原生 HTML 表格渲染**：在 yEd 中使用原生 HTML Table 还原节点，完美重现 Graphviz 中的表格视觉结构。
*   **保留布局与样式**：自动提取 Graphviz 的布局坐标并进行智能缩放防重叠；100% 还原 LLVM 连线样式（例如蓝虚线表示控制流，红粗线表示 Glue）。
*   **自适应节点大小**：文本节点注入自适应策略，完美包裹内部表格，拒绝文字溢出。

### 🛠 环境依赖
*   Python 3.x
*   安装 [Graphviz](https://graphviz.org/) 确保 `dot` 命令已加入系统环境变量 PATH 中。

### 🚀 使用方法

```bash
python3 dot2xml.py input.dot output.graphml
```

1. 使用 LLVM 生成你的 DAG dot 文件（例如通过 `llc -view-isel-dags`）。
2. 运行上述转换脚本。
3. 使用 yEd 软件打开生成的 `output.graphml` 文件即可自由拖拽编辑。

### 📂 示例 (Examples)
你可以进入 `/examples` 目录，查看 LLVM 生成的原始 `main.dot` 文件以及通过脚本转换后精美的 `main.graphml` 文件。

---
**License**: MIT License
